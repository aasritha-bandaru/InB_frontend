pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Replace Unix shell command with Windows command
                bat 'mvn clean package' // Use 'mvn' command for Maven build
            }
        }
    }
}